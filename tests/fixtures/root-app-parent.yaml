apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root-app
  namespace: argocd
spec:
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  source:
    repoURL: ./parent-chart
    targetRevision: HEAD
    helm:
      releaseName: root-release
  project: default
